import React from 'react';

export const CartListProperties = ({ settings, onUpdate }) => {
    return (
        <div className="p-4 text-sm text-slate-500">
            Cart List Properties (Coming Soon)
        </div>
    );
};
